(function() {
    var release = TP.sys.release;
    /* eslint-disable */

    //  --- latest.js start ---
    release({
        "describe": "{{describe}}",
        "major": "{{major}}",
        "minor": "{{minor}}",
        "patch": "{{patch}}",
        "suffix": "{{suffix}}",
        "ptag": "{{ptag}}",
        "commits": "{{commits}}",
        "phash": "{{phash}}",
        "time": "{{time}}",
        "semver": "{{semver}}"
    });
    //  --- end latest.js ---

}());
