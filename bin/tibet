#!/usr/bin/env node

/**
 * @overview Top-level TIBET command-line script. All the real work is performed
 * by the CLI object imported by this script.
 * @author Scott Shattuck (ss)
 * @copyright Copyright (C) 1999-2014 Technical Pursuit Inc. (TPI) All Rights
 *     Reserved. Patents Pending, Technical Pursuit Inc. Licensed under the
 *     OSI-Approved Reciprocal Public License (RPL) Version 1.5. See the RPL
 *     for your rights and responsibilities. Contact TPI to purchase optional
 *     open source waivers to keep your derivative work source code private.
 */

var cli = require('../src/cli/_cli.js');

var options = {};

//  Capture npm metadata about TIBET installation for downstream use.
require('child_process').exec('npm ll -g tibet --json',
    function(err, stdout, stderr) {
        if (err) {
            require('chalk');
            console.log(chalk.red(
                'Unable to locate TIBET installation: ' + stderr));
            process.exit(1);
        }

        // Run the cli once we've found TIBET.
        options.tibet = JSON.parse(stdout);
        cli.run(options);
    });

